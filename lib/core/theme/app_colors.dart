import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFACC15); // Yellow-400 (Accent)
  static const Color card = Color(0xFFFEF08A); // Yellow-200
  static const Color accent = Color(0xFF111827); // Gray-900 (CTA)
  static const Color background = Color(0xFFF9FAFB); // Neutral Gray
  static const Color text = Color(0xFF0F172A); // Slate-900
  static const Color textMuted = Color(0xFF64748B); // Slate-500
  static const Color secondary = Color(0xFFE2E8F0); // Slate-200 (For dots/dividers)
  static const Color white = Color(0xFFFFFFFF);
  static const Color success = Color(0xFF10B981); // Emerald-500
  static const Color error = Color(0xFFEF4444); // Red-500

  // Spacing (from MASTER.md)
  static const double spaceXS = 4.0;
  static const double spaceSM = 8.0;
  static const double spaceMD = 16.0;
  static const double spaceLG = 24.0;
  static const double spaceXL = 32.0;
  static const double space2XL = 48.0;
  static const double space3XL = 64.0;
}
